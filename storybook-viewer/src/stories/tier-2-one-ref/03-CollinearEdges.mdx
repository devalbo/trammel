import { Meta } from '@storybook/blocks';
import * as Stories from './03-CollinearEdges.stories';
import { LiveCanvas } from '../../components/LiveCanvas';

<Meta of={Stories} />

# Collinear Edges

Two rects at different Y positions whose left edges are forced to the same X coordinate using `left="#top.left"`.

## Elements

| Element | Type | Purpose |
|---------|------|---------|
| `<Rect>` #top | Primitive | Reference shape |
| `<Rect>` #bottom | Primitive | Left edge aligned to top's left |

## Default

<LiveCanvas code={Stories.defaultCode} />

## Resolver Trace

1. top: left = 40
2. bottom: left = `#top.left` = 40 — x = 40

## What This Validates

- The simplest form of edge alignment — collinear left edges
- `left="#id.left"` aligns left edges (same X)
- Shapes can differ in width/height while maintaining edge alignment
